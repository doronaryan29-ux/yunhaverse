{
  "name": "yunhaverse-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n LARAVEL,FRONTEND -c blue,green \"npm run dev:server\" \"npm run dev:frontend\"",
    "dev:server": "concurrently -n serve,queue -c \"#93c5fd,#c4b5fd\" \"cd server-laravel && php artisan serve\" \"cd server-laravel && php artisan queue:listen --tries=1 --timeout=0\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "preview": "npm --prefix frontend run preview",
    "postinstall": "npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
